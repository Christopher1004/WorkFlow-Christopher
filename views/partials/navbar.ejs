<nav class="navbar">
    <div class="container">
        <div class="logo">
            <a href="/">
                <img class="logo" src="/assets/image/logoWorkFlow-com-nome.svg" alt="logoWorkFlow">
            </a>
            <div class="hamburger">
                <span></span>
                <span></span>
                <span></span>
            </div>
        </div>
        <div class="links">
            <a href="/" class="text-xs projetos <%= currentRoute === '/' ? 'active' : '' %>">Projetos</a>
            <a href="/category"
                class="text-xs  categorias <%= currentRoute === '/category' ? 'active' : '' %>">Categorias</a>
            <a href="/propostas"
                class="text-xs  propostas <%= currentRoute === '/propostas' ? 'active' : '' %>">Propostas</a>
            <a href="/freelancers"
                class="text-xs  freelancers <%= currentRoute === '/freelancers' ? 'active' : '' %>">Freelancers</a>
        </div>
        <div class="searchbarContainer">
            <div class="searchbar">
                <img src="/assets/image/search-icon.svg" alt="searchIcon" class="icon">
                <input type="text" class="text-xs searchbar-input shadow-0" id="searchInput" placeholder="Buscar projeto...">
            </div>
        </div>
        <div class="buttonsContainer">
            <a class="aLogin" id="aLogin" href="/login">
                <button class="btn btn-ghost btn-white text-base buttonNav entrar" id="btnEntrar">Entrar</button>
            </a>
            <a class="aRegister" id="aRegister" href="/register">
                <button class="btn btn-purple text-sm registrar buttonNav" id="btnCriarConta">Criar Conta</button>
            </a>
            <div id="userControls" class="userControls" style="display:none; align-items: center; gap: 12px;">
                <button id="btnAdd" class="buttonAdd btn btn-purple" title="Adicionar novo">Criar</button>
                <div class="mobile-only w-full flex-col gap-2">
                    <button class="btn btn-gray w-full">Meu Perfil</button>
                    <button class="btn btn-danger w-full">Sair</button>
                </div>
                <div class="container-row flex flex-row gap-2">
                    <div class="containerNotifyChat flex flex-row gap-2">
                        <div class="notificacoes">
                            <button class="btnNotificacoes btn btn-gray"><ion-icon name="notifications-outline" style="color: var(--white); font-size: 25px;"></ion-icon></button>
                        </div>
                        <div class="linhaDivisoria"></div>
                        <div class="chat">
                            <button class="btn btn-gray" onclick="window.location.href='/chat'" style="position: relative;"><ion-icon name="chatbubbles-outline" style="color: var(--white); font-size: 25px;"></ion-icon></button>
                        </div>
                    </div>
                    <div id="userPhoto" class="userPhoto"></div>
                </div>

            </div>
        </div>

    </div>
</nav>
<div id="dropDownMenu" class="dropDownMenu mg-t-6 mg-r-4" style="display: none;">
    <div class="user-info flex items-center">
        <img src="" id="userPhotoDrop" alt="Foto de perfil" class="profile-img">
        <span class="user-name h-full">Nome Usuário</span>
    </div>
    <div id="linha" style="width: 100%; height: 2px; background-color: #3c38a6;"></div>
    <a href="/perfil">Perfil</a>
    <a href="/configuracoes">Configurações</a>
    <a href="" id="dropDownLogout">Sair</a>
</div>

<style>
    .navbar {
        position: fixed;
        top: 0;
        width: 100%;
        z-index: 1001;
    }

    .navbar.transparent-bg {
        background-color: transparent;
        box-shadow: inset 0 -200px 10px rgba(0, 0, 0, 0.1);
    }

    .linhaDivisoria {
        width: 1px;
        background: rgba(255, 255, 255, 0.2);
        height: 25px;
        align-self: center;
    }

    .userPhoto:hover {
        border: 3px solid #6f6dcc;
        transition: 0.3s ease;
    }

    .user-info {
        display: flex;
        align-items: center;
        gap: 10px;
        padding: 10px;
    }

    .profile-img {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        object-fit: cover;
    }

    .dropDownMenu {
        position: absolute;
        z-index: 1;
        top: 60px;
        right: 0;
        min-width: 180px;
        background-color: #1A1A1A;
        border-radius: 12px;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.4);
        border: 1px solid #2a2a2a;
        z-index: 1000;
        overflow: hidden;
        font-family: 'Segoe UI', sans-serif;
        animation: fadeIn 0.2s ease;
        width: 15%;
    }

    .dropDownMenu a {
        display: block;
        padding: 12px 16px;
        color: #fff;
        text-decoration: none;
        font-size: 15px;
        border: 1px solid #2a2a2a;
        transition: background 0.2s ease, color 0.2s ease;
    }

    .dropDownMenu a:hover {
        background-color: #3c38a6;
        color: white;
    }

    .dropDownMenu::before {
        content: "";
        position: absolute;
        top: -8px;
        left: 20px;
        width: 0;
        height: 0;
        border-left: 8px solid transparent;
        border-right: 8px solid transparent;
        border-bottom: 8px solid #1A1A1A;
        filter: drop-shadow(0 -1px 1px rgba(0, 0, 0, 0.04));
    }

    @keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(-5px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .popup-overlay {
        display: none;
        position: fixed;
        top: 0;
        left: 0;
        width: 100vw;
        height: 100vh;
        background: rgba(0, 0, 0, 0.6);
        z-index: 9999;
        justify-content: center;
        align-items: center;
    }

    .popup {
        background: white;
        padding: 30px;
        border-radius: 12px;
        text-align: center;
        width: 300px;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.3);
    }

    .popup input[type="file"] {
        margin: 20px 0;
        width: 100%;
    }

    .popup button {
        padding: 10px 20px;
        margin: 5px;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        font-weight: bold;
    }

    #uploadButton {
        background-color: #4CAF50;
        color: white;
    }

    .close-btn {
        background-color: #f44336;
        color: white;
    }

    .links a {
        position: relative;
        color: rgba(150, 150, 150, 0.4) !important;
        transition: color 0.3s ease;
        padding-bottom: 5px;
        overflow: hidden;
    }

    .links a::after {
        content: '';
        position: absolute;
        bottom: 0;
        left: 0;
        width: 100%;
        height: 2px;
        background-color: #3c38a6;
        transform: scaleX(0);
        transform-origin: bottom left;
        transition: transform 0.3s ease-out;
    }


    .links a.active::after,
    .links a:hover::after {
        transform: scaleX(1);
    }

    .links {
        display: flex;
        align-items: center;
        gap: 20px;
        padding-bottom: 5px;
    }

    .links a:hover,
    .links a.active {
        color: white !important;
    }

    .searchbar {
        background: transparent !important;
        border: 1px solid rgba(150, 150, 150, 0.4) !important;
        border-radius: 8px;
        display: flex;
        align-items: center;
        padding: 4px 10px;
        transition: border-color 0.3s ease;
    }

    .searchbar:hover,
    .searchbar:focus-within {
        border-color: #aaaaaa;
    }

    .searchbar-input {
        background: transparent !important;
        border: none;
        color: white;
        outline: none;
        font-size: 14px;
        padding: 6px 0;
        width: 100%;
    }

    .searchbar-input::placeholder {
        color: rgba(200, 200, 200, 0.5);
    }

    .searchbar .icon {
        width: 20px;
        height: 20px;
        margin-right: 8px;
        filter: brightness(0) invert(0.7);
    }
  .containerNotifyChat .btnNotificacoes,
.containerNotifyChat .chat button {
  background: transparent !important;
  border: none !important;
  box-shadow: none !important;
  border-radius: 50%;
  padding: 8px;
  transition:
    transform 0.25s cubic-bezier(0.4, 0, 0.2, 1),
    box-shadow 0.3s ease,
    filter 0.3s ease;
  will-change: transform, box-shadow, filter;
  outline-offset: 4px;
  transform-origin: center center; 
}

.containerNotifyChat .btnNotificacoes:hover,
.containerNotifyChat .chat button:hover {
  transform: scale(1.12);
  box-shadow:
    0 8px 15px rgba(108, 109, 204, 0.35),
    0 0 10px 3px rgba(108, 109, 204, 0.3);
  filter: drop-shadow(0 0 6px rgba(108, 109, 204, 0.6));
  cursor: pointer;
}

/* Foco */
.containerNotifyChat .btnNotificacoes:focus,
.containerNotifyChat .chat button:focus {
  outline: none;
  outline-offset: 3px;
}

/* Animação de balanço do sino */
@keyframes sinoShake {
  0%, 100% {
    transform: rotate(0deg);
  }
  15% {
    transform: rotate(15deg);
  }
  30% {
    transform: rotate(-10deg);
  }
  45% {
    transform: rotate(8deg);
  }
  60% {
    transform: rotate(-5deg);
  }
  75% {
    transform: rotate(3deg);
  }
}

.containerNotifyChat .btnNotificacoes:hover ion-icon {
  animation: sinoShake 0.8s ease-in-out infinite;
  transform-origin: center bottom; 
  cursor: pointer;
}

</style>
<script type="module" src="/js/navBar-auth.js"></script>
<script>
    document.addEventListener('DOMContentLoaded', () => {
        const input = document.querySelector('.searchbar-input');
        if (!input) return;

        input.addEventListener('keypress', function (e) {
            if (e.key === 'Enter') {
                const valor = input.value.trim();
                const path = window.location.pathname;

                if (valor.startsWith('#projetos')) {
                    const termo = valor.replace('#projetos', '').trim();
                    window.location.href = `/?busca=${encodeURIComponent(termo)}`;
                } else if (valor.startsWith('#freelancers')) {
                    const termo = valor.replace('#freelancers', '').trim();
                    window.location.href = `/freelancers?busca=${encodeURIComponent(termo)}`;
                } else if (valor.startsWith('#propostas')) {
                    const termo = valor.replace('#propostas', '').trim();
                    window.location.href = `/propostas?busca=${encodeURIComponent(termo)}`;
                } else {
                    const termoBusca = encodeURIComponent(valor);
                    if (path.includes('/freelancers')) {
                        window.location.href = `/freelancers?busca=${termoBusca}`;
                    } else if (path.includes('/propostas')) {
                        window.location.href = `/propostas?busca=${termoBusca}`;
                    } else if (path.includes('/projetos')) {
                        window.location.href = `/?busca=${termoBusca}`;
                    }
                }
            }
        });
    });
</script>
<script>
    document.addEventListener('DOMContentLoaded', () => {
        const navbar = document.querySelector('.navbar');
        const currentPagePath = window.location.pathname;

        if (navbar) {
            if (currentPagePath.includes('/perfil')) {
                navbar.classList.add('transparent-bg');
            } else {
                navbar.classList.remove('transparent-bg');
            }
        }
    });
</script>